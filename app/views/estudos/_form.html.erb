<%= form_for(@estudo, multpart: true) do |f| %>
  <% if @estudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estudo.errors.count, "error") %> prohibited this estudo from being saved:</h2>

      <ul>
      <% @estudo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, (t 'title') %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image, (t 'image') %>
    <%= f.file_field :image %>
  </div>
  <br>
  <div class="field">
    <%= f.label :description, (t 'description') %>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :buttom_color, (t 'buttom_color') %>
    <%= f.text_field :buttom_color %>
  </div>
  


  
  <div class="actions">
    <%= f.submit class: 'btn btn-primary btn-lg btn-block' %>
  </div>
<% end %>
