<%= form_for(@survey_version) do |f| %>
  <% if @survey_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_version.errors.count, "error") %> prohibited this survey_version from being saved:</h2>

      <ul>
      <% @survey_version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :survey %><br>
    <%= f.text_field :survey %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :file %><br>
    <%= f.text_field :file %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
